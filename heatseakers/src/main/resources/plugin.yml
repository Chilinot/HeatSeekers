name: ${project.name}
author: LucasEmanuel
version: ${project.version}
main: ${project.groupId}.${project.artifactId}.Main

permissions:
  heatseekers.craft:
    default: op
    description: Allows a player to craft the weapon.
  heatseekers.block:
    default: false
    description: Blocks a player from using the plugin.